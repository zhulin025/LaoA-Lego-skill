interface:
  display_name: "LEGO Model"
  short_description: "Create editable or reference-faithful micro-brick 3D models."
  default_prompt: "Use $lego-model to reproduce this reference as a detailed interactive brick-built 3D model."
